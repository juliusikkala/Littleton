#version 430

// Vector size
layout(local_size_x=MATRIX_HEIGHT) in;

layout(std430, binding=1) buffer input_matrix
{
    readonly MATRIX_TYPE data[MATRIX_WIDTH][MATRIX_HEIGHT];
};

void main() {
    // Do nothing for now
}
